{% unless product.empty? %}
  <script type="application/json"
    class="ProductJson"
    data-product-handle="{{ product.handle }}"
    id="ProductJson-{{ product.handle }}">
      {{ product | json }}
  </script>
{% endunless %}
